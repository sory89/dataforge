name: dataforge
version: "1.0.0"
profile: dataforge

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  dataforge:
    staging:
      +materialized: view
    marts:
      +materialized: table
