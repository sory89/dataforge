version: 2

models:
  - name: stg_orders
    description: Commandes nettoyées (hors annulations)
    columns:
      - name: order_id
        tests: [unique, not_null]
      - name: amount_eur
        tests:
          - not_null
          - positive_value
